const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const I={
fi:{brand:'Kampuskeikat',authHero:'Käytännön työt yhdessä paikassa.',authIntro:'Kaapelointi, laiteasennukset ja palvelintyöt – tehtävästä valmiiksi.',login:'Kirjaudu',register:'Rekisteröidy',email:'Sähköposti',password:'Salasana',password10:'Salasana (vähintään 10 merkkiä)',name:'Nimi',group:'Ryhmä',invite:'Ryhmän kutsukoodi',logout:'Kirjaudu ulos',overview:'Yleiskuva',tasks:'Tehtävät',work:'Työtilanne',students:'Opiskelijat',available:'Avoimet tehtävät',history:'Omat työt',newTask:'Uusi tehtävä',active:'Aktiiviset',waiting:'Odottaa kuittausta',done:'Valmiit',locations:'Kohteet',enroll:'Ilmoittaudu',start:'Aloita työ',open:'Avaa',edit:'Muokkaa',archive:'Arkistoi',complete:'Merkitse valmiiksi',returnWork:'Palauta työhön',note:'Työseloste ja huomiot',photos:'Valokuvat',save:'Tallenna',submit:'Ilmoita valmiiksi',addPhotos:'Lisää kuvia',location:'Kohde',room:'Tila / huone',schedule:'Ajankohta',deadline:'Takaraja',capacity:'Tekijöiden enimmäismäärä',category:'Työn tyyppi',description:'Työohje',title:'Otsikko',openEnrollment:'Ilmoittautuminen avoinna',cable:'Kaapelointi',classroom:'Tietokoneluokka',server:'Palvelin',network:'Verkko',hardware:'Laiteasennus',other:'Muu',enrolled:'Ilmoittautunut',in_progress:'Työn alla',ready:'Odottaa kuittausta',completed:'Valmis',closed:'Suljettu',student:'Opiskelija',status:'Tila',actions:'Toiminnot',all:'Kaikki',search:'Hae...',noTasks:'Tehtäviä ei ole vielä.',noWork:'Ei töitä tässä tilassa.',noStudents:'Opiskelijoita ei ole vielä.',assign:'Määritä opiskelijalle',create:'Luo tehtävä',update:'Tallenna muutokset',photoHelp:'Enintään 5 kuvaa, 5 Mt/kuva. JPEG, PNG tai WebP.',selfEnroll:'Opiskelija voi ilmoittautua itse. Opettaja voi myös määrittää tekijän.',success:'Tallennettu',confirmArchive:'Arkistoidaanko tehtävä?',taskInfo:'Tehtävän tiedot'},
en:{brand:'Campus Work',authHero:'Practical work in one place.',authIntro:'Cabling, equipment and server installations — from assignment to completion.',login:'Log in',register:'Sign up',email:'Email',password:'Password',password10:'Password (at least 10 characters)',name:'Name',group:'Group',invite:'Group invite code',logout:'Log out',overview:'Overview',tasks:'Tasks',work:'Work status',students:'Students',available:'Open tasks',history:'My work',newTask:'New task',active:'Active',waiting:'Awaiting confirmation',done:'Completed',locations:'Locations',enroll:'Enroll',start:'Start work',open:'Open',edit:'Edit',archive:'Archive',complete:'Mark completed',returnWork:'Return to work',note:'Work notes and observations',photos:'Photos',save:'Save',submit:'Submit as ready',addPhotos:'Add photos',location:'Location',room:'Room',schedule:'Scheduled',deadline:'Deadline',capacity:'Maximum workers',category:'Work type',description:'Instructions',title:'Title',openEnrollment:'Self-enrollment open',cable:'Cabling',classroom:'Computer classroom',server:'Server',network:'Network',hardware:'Equipment installation',other:'Other',enrolled:'Enrolled',in_progress:'In progress',ready:'Awaiting confirmation',completed:'Completed',closed:'Closed',student:'Student',status:'Status',actions:'Actions',all:'All',search:'Search...',noTasks:'No tasks yet.',noWork:'No work in this state.',noStudents:'No students yet.',assign:'Assign to student',create:'Create task',update:'Save changes',photoHelp:'Up to 5 photos, 5 MB each. JPEG, PNG or WebP.',selfEnroll:'Students can self-enroll. The teacher can also assign them.',success:'Saved',confirmArchive:'Archive this task?',taskInfo:'Task details'},
uk:{brand:'Практичні роботи',authHero:'Усі практичні роботи в одному місці.',authIntro:'Прокладання кабелів, монтаж обладнання та серверів — від завдання до виконання.',login:'Увійти',register:'Реєстрація',email:'Електронна пошта',password:'Пароль',password10:'Пароль (щонайменше 10 символів)',name:'Ім’я',group:'Група',invite:'Код запрошення групи',logout:'Вийти',overview:'Огляд',tasks:'Завдання',work:'Стан робіт',students:'Студенти',available:'Відкриті завдання',history:'Мої роботи',newTask:'Нове завдання',active:'Активні',waiting:'Чекають підтвердження',done:'Виконані',locations:'Об’єкти',enroll:'Записатися',start:'Почати роботу',open:'Відкрити',edit:'Редагувати',archive:'Архівувати',complete:'Позначити виконаним',returnWork:'Повернути в роботу',note:'Опис виконаної роботи та примітки',photos:'Фотографії',save:'Зберегти',submit:'Надіслати на підтвердження',addPhotos:'Додати фото',location:'Об’єкт',room:'Аудиторія / кімната',schedule:'Час початку',deadline:'Кінцевий термін',capacity:'Максимум виконавців',category:'Тип роботи',description:'Інструкція',title:'Назва',openEnrollment:'Самостійний запис відкритий',cable:'Кабелі',classroom:'Комп’ютерний клас',server:'Сервер',network:'Мережа',hardware:'Монтаж обладнання',other:'Інше',enrolled:'Записано',in_progress:'У роботі',ready:'Чекає підтвердження',completed:'Виконано',closed:'Закрито',student:'Студент',status:'Статус',actions:'Дії',all:'Усі',search:'Пошук...',noTasks:'Завдань поки немає.',noWork:'У цьому статусі робіт немає.',noStudents:'Студентів поки немає.',assign:'Призначити студенту',create:'Створити завдання',update:'Зберегти зміни',photoHelp:'До 5 фото по 5 МБ. JPEG, PNG або WebP.',selfEnroll:'Студент може записатися сам. Викладач також може призначити виконавця.',success:'Збережено',confirmArchive:'Архівувати завдання?',taskInfo:'Дані завдання'}};
let state={lang:'fi',theme:matchMedia('(prefers-color-scheme:dark)').matches?'dark':'light',me:null,csrf:'',route:'overview',tasks:[],work:[],students:[]};
const t=k=>I[state.lang][k]||k;
function esc(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function fmt(v){if(!v)return '—';return new Intl.DateTimeFormat(state.lang==='uk'?'uk-UA':state.lang==='en'?'en-GB':'fi-FI',{dateStyle:'medium',timeStyle:'short'}).format(new Date(v))}
function langs(){return ['fi','en','uk'].map(x=>`<button class="${state.lang===x?'active':''}" data-lang="${x}" aria-label="${x.toUpperCase()}">${x.toUpperCase()}</button>`).join('')}
function translate(){document.documentElement.lang=state.lang;$$('[data-t]').forEach(e=>e.textContent=t(e.dataset.t));$$('.langs').forEach(e=>e.innerHTML=langs());document.documentElement.dataset.theme=state.theme}
async function api(url,opt={}){opt.headers=opt.headers||{};if(opt.body&&!(opt.body instanceof FormData)){opt.headers['Content-Type']='application/json';opt.body=JSON.stringify(opt.body)}if(!['GET','HEAD'].includes(opt.method||'GET')&&state.csrf)opt.headers['X-CSRF-Token']=state.csrf;let r=await fetch(url,opt);if(r.status===401){showAuth();throw Error('Sign in required')}if(!r.ok){let x=await r.json().catch(()=>({}));throw Error(x.detail||`HTTP ${r.status}`)}return r.status===204?null:r.json()}
function toast(s){let e=$('#toast');e.textContent=s;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),2300)}
function error(e){toast(e.message||String(e))}
function showAuth(){state.me=null;$('#app').hidden=true;$('#auth').hidden=false;translate()}
function nav(){let items=state.me.role==='admin'?[['overview','overview'],['tasks','tasks'],['users','users'],['teacher-requests','teacherRequests'],['groups','groups'],['audit','audit']]:state.me.role==='teacher'?[['overview','overview'],['tasks','tasks'],['work','work'],['students','students']]:[['overview','overview'],['tasks','available'],['history','history']];$('#nav').innerHTML=items.map(([r,l])=>`<button data-route="${r}" class="${state.route===r?'active':''}">${t(l)}</button>`).join('')}
async function showApp(){$('#auth').hidden=true;$('#app').hidden=false;$('#userName').textContent=state.me.name;state.route='overview';await go(state.route)}
async function go(route){state.route=route;nav();$('#view').innerHTML=`<div class="page"><div class="empty">…</div></div>`;try{if(state.me.role==='admin') await adminView(route);else if(state.me.role==='teacher') await teacherView(route);else await studentView(route)}catch(e){error(e)}nav()}
function head(title,sub='',action=''){return `<div class="page-head"><div><h1>${esc(title)}</h1>${sub?`<p class="sub">${esc(sub)}</p>`:''}</div>${action}</div>`}
function empty(text){return `<div class="empty"><svg viewBox="0 0 48 48"><path d="M8 15h32v25H8zM15 15V9h18v6M16 25h16M16 31h10"/></svg><p>${esc(text)}</p></div>`}
function badge(s){return `<span class="badge ${s}">${esc(t(s))}</span>`}
const cat=c=>t(c)||c;
function taskCard(x,teacher=false){let e=x.my_enrollment;let status=e?badge(e.status):(!x.is_open?badge('closed'):'');return `<article class="card"><div class="eyebrow">${esc(cat(x.category))} ${status}</div><h2>${esc(x.title)}</h2><p>${esc(x.description)}</p><div class="meta"><span>${t('location')}: <strong>${esc([x.location,x.room].filter(Boolean).join(' · ')||'—')}</strong></span><span>${t('schedule')}: <strong>${fmt(x.scheduled_at)}</strong></span><span>${esc(x.enrolled_count)}${x.capacity?' / '+x.capacity:''}</span></div><div class="actions">${teacher?`<button class="secondary" data-edit="${x.id}">${t('edit')}</button><button class="secondary" data-assign="${x.id}">${t('assign')}</button><button class="danger-btn" data-archive="${x.id}">${t('archive')}</button>`:e?`<button class="primary" data-open-enrollment="${e.id}">${t('open')}</button>`:(x.is_open?`<button class="primary" data-enroll="${x.id}">${t('enroll')}</button>`:'')}</div></article>`}
async function loadTasks(){state.tasks=await api('/api/tasks')}
async function teacherView(r){
 if(r==='overview'){let w=await api('/api/admin/enrollments'),tasks=await api('/api/tasks');let active=w.filter(x=>['enrolled','in_progress'].includes(x.status)).length,ready=w.filter(x=>x.status==='ready').length,done=w.filter(x=>x.status==='completed').length,loc=new Set(tasks.map(x=>x.location).filter(Boolean)).size;$('#view').innerHTML=`<div class="page">${head(t('overview'))}<div class="stats"><div class="stat"><small>${t('active')}</small><strong>${active}</strong></div><div class="stat"><small>${t('waiting')}</small><strong>${ready}</strong></div><div class="stat"><small>${t('done')}</small><strong>${done}</strong></div><div class="stat"><small>${t('locations')}</small><strong>${loc}</strong></div></div><h2>${t('waiting')}</h2><div class="grid" style="margin-top:1rem">${w.filter(x=>x.status==='ready').map(workCard).join('')||empty(t('noWork'))}</div></div>`;}
 if(r==='tasks'){await loadTasks();$('#view').innerHTML=`<div class="page">${head(t('tasks'),t('selfEnroll'),`<button class="primary" data-new-task>${t('newTask')}</button>`)}<div class="grid">${state.tasks.map(x=>taskCard(x,true)).join('')||empty(t('noTasks'))}</div></div>`}
 if(r==='work'){state.work=await api('/api/admin/enrollments');$('#view').innerHTML=`<div class="page">${head(t('work'))}<div class="filters"><select id="statusFilter"><option value="">${t('all')} · ${t('status')}</option>${['enrolled','in_progress','ready','completed'].map(s=>`<option value="${s}">${t(s)}</option>`).join('')}</select><select id="taskFilter"><option value="">${t('all')} · ${t('tasks')}</option>${[...new Map(state.work.map(x=>[x.task.id,x.task])).values()].map(x=>`<option value="${x.id}">${esc(x.title)}</option>`).join('')}</select><input id="workSearch" placeholder="${t('search')}"></div><div id="workTable">${workTable(state.work)}</div></div>`}
 if(r==='students'){state.students=await api('/api/admin/students');$('#view').innerHTML=`<div class="page">${head(t('students'))}${studentsTable()}</div>`}
}
async function adminView(r){
 if(r==='overview'){let users=await api('/api/admin/users'),requests=await api('/api/admin/teacher-requests'),groups=await api('/api/admin/groups'),logs=await api('/api/admin/audit-log');$('#view').innerHTML=`<div class="page">${head(t('overview'))}<div class="stats"><div class="stat"><small>${t('users')}</small><strong>${users.length}</strong></div><div class="stat"><small>${t('teacherRequests')}</small><strong>${requests.filter(x=>x.status==='pending').length}</strong></div><div class="stat"><small>${t('groups')}</small><strong>${groups.filter(x=>x.is_active).length}</strong></div><div class="stat"><small>${t('audit')}</small><strong>${logs.length}</strong></div></div><h2>${t('teacherRequests')}</h2><div class="grid">${requests.filter(x=>x.status==='pending').map(teacherRequestCard).join('')||empty(t('noWork'))}</div></div>`}
 if(r==='tasks'){await loadTasks();$('#view').innerHTML=`<div class="page">${head(t('tasks'),t('selfEnroll'),`<button class="primary" data-new-task>${t('newTask')}</button>`)}<div class="grid">${state.tasks.map(x=>taskCard(x,true)).join('')||empty(t('noTasks'))}</div></div>`}
 if(r==='users'){let users=await api('/api/admin/users');$('#view').innerHTML=`<div class="page">${head(t('users'))}<div class="table-wrap"><table><thead><tr><th>${t('name')}</th><th>${t('email')}</th><th>${t('role')}</th><th>${t('status')}</th><th>${t('actions')}</th></tr></thead><tbody>${users.map(x=>`<tr><td>${esc(x.name)}</td><td>${esc(x.email)}</td><td>${esc(x.role)}</td><td>${x.active?t('active'):t('blocked')}</td><td>${x.active?`<button class="danger-btn" data-block-user="${x.id}">${t('block')}</button>`:`<button class="secondary" data-unblock-user="${x.id}">${t('unblock')}</button>`} <button class="secondary" data-reset-user="${x.id}">${t('resetPassword')}</button></td></tr>`).join('')}</tbody></table></div></div>`}
 if(r==='teacher-requests'){let rows=await api('/api/admin/teacher-requests');$('#view').innerHTML=`<div class="page">${head(t('teacherRequests'))}<div class="grid">${rows.map(teacherRequestCard).join('')||empty(t('noWork'))}</div></div>`}
 if(r==='groups'){let groups=await api('/api/admin/groups');$('#view').innerHTML=`<div class="page">${head(t('groups'),'',`<button class="primary" data-new-group>${t('createGroup')}</button>`)}<div class="grid">${groups.map(groupCard).join('')||empty(t('groups'))}</div></div>`}
 if(r==='audit'){let rows=await api('/api/admin/audit-log');$('#view').innerHTML=`<div class="page">${head(t('audit'))}<div class="table-wrap"><table><thead><tr><th>${t('status')}</th><th>${t('actions')}</th><th>${t('student')}</th><th>${t('schedule')}</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${fmt(x.created_at)}</td><td>${esc(x.action)}</td><td>${esc(x.target_type)} #${esc(x.target_id||'—')}</td><td>${esc(x.details)}</td></tr>`).join('')}</tbody></table></div></div>`}
}
function teacherRequestCard(x){return `<article class="card"><div class="eyebrow">${esc(x.status)}</div><h2>${esc(x.user_name)}</h2><p>${esc(x.note||'')}</p>${x.status==='pending'?`<div class="actions"><button class="primary" data-approve-request="${x.id}">${t('approve')}</button><button class="danger-btn" data-reject-request="${x.id}">${t('reject')}</button></div>`:''}</article>`}
function groupCard(x){return `<article class="card"><div class="eyebrow">${x.is_active?t('active'):t('blocked')}</div><h2>${esc(x.name)}</h2><p>${esc(x.description||'')}</p><div class="meta"><span>${t('student')}: ${x.member_count}</span><span>${t('waiting')}: ${x.pending_count}</span><span>${t('teacher')}: ${x.teacher_count}</span></div><div class="actions"><button class="secondary" data-members="${x.id}">${t('students')}</button><button class="secondary" data-teachers="${x.id}">${t('teachers')}</button></div></article>`}
function groupForm(){return `<form id="groupForm" class="form-grid"><label class="wide">${t('name')}<input name="name" required minlength="1" maxlength="120"></label><label class="wide">${t('description')}<textarea name="description"></textarea></label><label class="wide">${t('invite')}<input name="join_code" minlength="1" maxlength="50"></label><label class="wide"><input name="is_active" type="checkbox" checked>${t('active')}</label><p class="form-error wide"></p><div class="modal-actions"><button type="button" class="secondary" data-close>×</button><button class="primary">${t('create')}</button></div></form>`}
function teacherAssignForm(groupId,teachers,assigned){let current=new Set(assigned.map(x=>x.teacher_id));return `<form id="teacherAssignForm" data-group="${groupId}" class="form-grid"><label class="wide">${t('teacher')}<select name="teacher_id" required>${teachers.filter(x=>x.role==='teacher'&&x.teacher_approved).map(x=>`<option value="${x.id}" ${current.has(x.id)?'disabled':''}>${esc(x.name)} · ${esc(x.email)}</option>`).join('')}</select></label><label class="wide">${t('permission')}<select name="permission"><option value="owner">owner</option><option value="assistant">assistant</option><option value="viewer">viewer</option></select></label><p class="form-error wide"></p><div class="modal-actions"><button type="button" class="secondary" data-close>×</button><button class="primary">${t('assign')}</button></div></form>`}
function workCard(x){return `<article class="card"><div class="eyebrow">${esc(x.student?.name||'')} ${badge(x.status)}</div><h2>${esc(x.task.title)}</h2><p>${esc([x.task.location,x.task.room].filter(Boolean).join(' · '))}</p><div class="actions"><button class="primary" data-review="${x.id}">${t('open')}</button></div></article>`}
function workTable(rows){return `<div class="table-wrap"><table><thead><tr><th>${t('student')}</th><th>${t('tasks')}</th><th>${t('location')}</th><th>${t('status')}</th><th>${t('actions')}</th></tr></thead><tbody>${rows.map(x=>`<tr><td><span class="student-name">${esc(x.student.name)}</span><br><small>${esc(x.student.group_name||'')}</small></td><td>${esc(x.task.title)}</td><td>${esc([x.task.location,x.task.room].filter(Boolean).join(' · ')||'—')}</td><td>${badge(x.status)}</td><td><button class="secondary" data-review="${x.id}">${t('open')}</button></td></tr>`).join('')}</tbody></table></div>`}
function studentsTable(){if(!state.students.length)return empty(t('noStudents'));return `<div class="table-wrap"><table><thead><tr><th>${t('student')}</th><th>${t('group')}</th><th>${t('active')}</th><th>${t('waiting')}</th><th>${t('done')}</th></tr></thead><tbody>${state.students.map(x=>`<tr><td><span class="student-name">${esc(x.name)}</span><br><small>${esc(x.email)}</small></td><td>${esc(x.group_name||'—')}</td><td>${(x.counts.enrolled||0)+(x.counts.in_progress||0)}</td><td>${x.counts.ready||0}</td><td>${x.counts.completed||0}</td></tr>`).join('')}</tbody></table></div>`}
async function studentView(r){
 if(r==='overview'){state.work=await api('/api/my-work');let active=state.work.filter(x=>['enrolled','in_progress'].includes(x.status)),ready=state.work.filter(x=>x.status==='ready'),done=state.work.filter(x=>x.status==='completed');$('#view').innerHTML=`<div class="page">${head(t('overview'))}<div class="stats"><div class="stat"><small>${t('active')}</small><strong>${active.length}</strong></div><div class="stat"><small>${t('waiting')}</small><strong>${ready.length}</strong></div><div class="stat"><small>${t('done')}</small><strong>${done.length}</strong></div></div><h2>${t('active')}</h2><div class="grid" style="margin-top:1rem">${active.map(myWorkCard).join('')||empty(t('noWork'))}</div></div>`}
 if(r==='tasks'){await loadTasks();$('#view').innerHTML=`<div class="page">${head(t('available'))}<div class="grid">${state.tasks.map(x=>taskCard(x)).join('')||empty(t('noTasks'))}</div></div>`}
 if(r==='history'){state.work=await api('/api/my-work');let current=state.work.filter(x=>x.status!=='completed'),done=state.work.filter(x=>x.status==='completed');$('#view').innerHTML=`<div class="page">${head(t('history'))}<h2>${t('active')}</h2><div class="grid" style="margin:1rem 0 2rem">${current.map(myWorkCard).join('')||empty(t('noWork'))}</div><h2>${t('done')}</h2><div class="grid" style="margin-top:1rem">${done.map(myWorkCard).join('')||empty(t('noWork'))}</div></div>`}
}
function myWorkCard(x){return `<article class="card"><div class="eyebrow">${esc(cat(x.task.category))} ${badge(x.status)}</div><h2>${esc(x.task.title)}</h2><p>${esc(x.task.description)}</p><div class="meta"><span>${t('location')}: <strong>${esc([x.task.location,x.task.room].filter(Boolean).join(' · ')||'—')}</strong></span><span>${t('schedule')}: <strong>${fmt(x.task.scheduled_at)}</strong></span></div><div class="actions"><button class="primary" data-open-enrollment="${x.id}">${t('open')}</button></div></article>`}
function openModal(title,body){$('#modalTitle').textContent=title;$('#modalBody').innerHTML=body;$('#modal').showModal()}
function taskForm(x={},groups=[]){return `<form id="taskForm" class="form-grid" data-id="${x.id||''}"><label class="wide">${t('title')}<input name="title" value="${esc(x.title||'')}" required minlength="3"></label><label>${t('category')}<select name="category">${['cable','classroom','server','network','hardware','other'].map(c=>`<option value="${c}" ${x.category===c?'selected':''}>${t(c)}</option>`).join('')}</select></label><label>${t('group')}<select name="academic_group_id" required>${groups.filter(g=>g.is_active).map(g=>`<option value="${g.id}" ${String(x.academic_group_id)===String(g.id)?'selected':''}>${esc(g.name)}</option>`).join('')}</select></label><label>${t('capacity')}<input name="capacity" type="number" min="1" max="500" value="${x.capacity||''}"></label><label>${t('location')}<input name="location" value="${esc(x.location||'')}"></label><label>${t('room')}<input name="room" value="${esc(x.room||'')}"></label><label>${t('schedule')}<input name="scheduled_at" type="datetime-local" value="${localDT(x.scheduled_at)}"></label><label>${t('deadline')}<input name="due_at" type="datetime-local" value="${localDT(x.due_at)}"></label><label class="wide">${t('description')}<textarea name="description">${esc(x.description||'')}</textarea></label><label class="wide" style="display:flex;grid-template-columns:auto 1fr;align-items:center"><input name="is_open" type="checkbox" style="width:auto" ${x.is_open!==false?'checked':''}>${t('openEnrollment')}</label><p class="form-error wide"></p><div class="modal-actions"><button type="button" class="secondary" data-close>×</button><button class="primary">${x.id?t('update'):t('create')}</button></div></form>`}
function localDT(v){if(!v)return '';let d=new Date(v),z=n=>String(n).padStart(2,'0');return `${d.getFullYear()}-${z(d.getMonth()+1)}-${z(d.getDate())}T${z(d.getHours())}:${z(d.getMinutes())}`}
function dataObj(form){return Object.fromEntries(new FormData(form))}
async function editTask(id){let x=state.tasks.find(t=>t.id===id);let groups=await api('/api/groups');if(!groups.length){toast(t('noGroups'));return}openModal(x?t('edit'):t('newTask'),taskForm(x||{},groups))}
async function assignModal(taskId){let students=await api('/api/admin/students');openModal(t('assign'),students.length?`<form id="assignForm" data-task="${taskId}"><label>${t('student')}<select name="student_id">${students.map(s=>`<option value="${s.id}">${esc(s.name)} · ${esc(s.group_name||'')}</option>`).join('')}</select></label><p class="form-error"></p><button class="primary">${t('assign')}</button></form>`:empty(t('noStudents')))}
async function openEnrollment(id,admin=false){let rows=admin?state.work:await api('/api/my-work'),x=rows.find(e=>e.id===id);if(!x)return;let editable=x.status!=='completed';openModal(x.task.title,`<div style="display:grid;gap:1rem"><div>${badge(x.status)}</div><div class="meta"><span>${t('location')}: <strong>${esc([x.task.location,x.task.room].filter(Boolean).join(' · ')||'—')}</strong></span><span>${t('schedule')}: <strong>${fmt(x.task.scheduled_at)}</strong></span></div><p>${esc(x.task.description)}</p><label>${t('note')}<textarea id="workNote" ${editable?'':'disabled'}>${esc(x.note||'')}</textarea></label><div><h3>${t('photos')}</h3><p class="sub">${t('photoHelp')}</p></div><div class="photo-grid">${x.photos.map(p=>`<div class="photo"><a href="${p.url}" target="_blank" rel="noopener"><img src="${p.url}" alt="${esc(p.name)}" loading="lazy"></a>${editable?`<button data-delete-photo="${p.id}" aria-label="Delete">×</button>`:''}</div>`).join('')}</div>${editable?`<label>${t('addPhotos')}<input id="photoInput" type="file" accept="image/jpeg,image/png,image/webp" multiple></label>`:''}<div class="modal-actions">${!admin&&x.status==='enrolled'?`<button class="secondary" data-start="${x.id}">${t('start')}</button>`:''}${editable?`<button class="secondary" data-save-work="${x.id}">${t('save')}</button>`:''}${!admin&&['enrolled','in_progress'].includes(x.status)?`<button class="primary" data-submit-work="${x.id}">${t('submit')}</button>`:''}${admin&&x.status!=='completed'?`<button class="primary" data-set-status="${x.id}:completed">${t('complete')}</button>`:''}${admin&&x.status==='completed'?`<button class="secondary" data-set-status="${x.id}:in_progress">${t('returnWork')}</button>`:''}</div></div>`)}
async function saveWork(id,submit=false){await api(`/api/enrollments/${id}/note`,{method:'PATCH',body:{note:$('#workNote').value}});let f=$('#photoInput');if(f&&f.files.length){let fd=new FormData();[...f.files].forEach(x=>fd.append('files',x));await api(`/api/enrollments/${id}/photos`,{method:'POST',body:fd})}if(submit)await api(`/api/enrollments/${id}/submit`,{method:'POST'});$('#modal').close();toast(t('success'));go(state.route)}
document.addEventListener('click',async e=>{let b=e.target.closest('button');if(!b)return;try{
 if(b.dataset.lang){state.lang=b.dataset.lang;translate();if(state.me)go(state.route)}
 if(b.hasAttribute('data-theme')){state.theme=state.theme==='dark'?'light':'dark';translate()}
 if(b.dataset.authTab){$$('[data-auth-tab]').forEach(x=>x.classList.toggle('active',x===b));$('#loginForm').hidden=b.dataset.authTab!=='login';$('#registerForm').hidden=b.dataset.authTab!=='register'}
 if(b.dataset.route)go(b.dataset.route);if(b.hasAttribute('data-close'))$('#modal').close();if(b.hasAttribute('data-new-task'))editTask();if(b.hasAttribute('data-new-group'))openModal(t('createGroup'),groupForm());if(b.dataset.edit)editTask(+b.dataset.edit);if(b.dataset.assign)assignModal(+b.dataset.assign);
 if(b.dataset.archive&&confirm(t('confirmArchive'))){await api(`/api/tasks/${b.dataset.archive}/archive`,{method:'POST'});go('tasks')}
 if(b.dataset.enroll){await api(`/api/tasks/${b.dataset.enroll}/enroll`,{method:'POST'});toast(t('success'));go('tasks')}
 if(b.dataset.openEnrollment)openEnrollment(+b.dataset.openEnrollment,false);if(b.dataset.review)openEnrollment(+b.dataset.review,true);
 if(b.dataset.start){await api(`/api/enrollments/${b.dataset.start}/start`,{method:'POST'});$('#modal').close();go(state.route)}
 if(b.dataset.saveWork)saveWork(+b.dataset.saveWork,false);if(b.dataset.submitWork)saveWork(+b.dataset.submitWork,true);
 if(b.dataset.setStatus){let [id,status]=b.dataset.setStatus.split(':');await api(`/api/admin/enrollments/${id}/status`,{method:'PATCH',body:{status}});$('#modal').close();go(state.route)}
 if(b.dataset.deletePhoto){await api(`/api/photos/${b.dataset.deletePhoto}`,{method:'DELETE'});$('#modal').close();go(state.route)}
 if(b.dataset.approveRequest){await api(`/api/admin/teacher-requests/${b.dataset.approveRequest}/approve`,{method:'POST',body:{action:'approve'}});toast(t('success'));go(state.route)}
 if(b.dataset.rejectRequest){await api(`/api/admin/teacher-requests/${b.dataset.rejectRequest}/reject`,{method:'POST',body:{action:'reject'}});toast(t('success'));go(state.route)}
 if(b.dataset.blockUser){await api(`/api/admin/users/${b.dataset.blockUser}/block`,{method:'POST'});go(state.route)}
 if(b.dataset.unblockUser){await api(`/api/admin/users/${b.dataset.unblockUser}/unblock`,{method:'POST'});go(state.route)}
 if(b.dataset.resetUser){let result=await api(`/api/admin/users/${b.dataset.resetUser}/reset-password`,{method:'POST',body:{action:'reset'}});openModal(t('resetPassword'),`<p>${t('temporaryPassword')}: <strong>${esc(result.temporary_password)}</strong></p>`)}
 if(b.dataset.members){let rows=await api(`/api/admin/groups/${b.dataset.members}/members`);openModal(t('students'),rows.length?`<div class="table-wrap"><table><thead><tr><th>${t('name')}</th><th>${t('email')}</th><th>${t('status')}</th><th>${t('actions')}</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${esc(x.name)}</td><td>${esc(x.email)}</td><td>${esc(x.status)}</td><td>${x.status==='pending'?`<button class="primary" data-approve-member="${x.id}" data-group="${b.dataset.members}">${t('approve')}</button>`:''}</td></tr>`).join('')}</tbody></table></div>`:empty(t('noStudents')))}
 if(b.dataset.teachers){let assigned=await api(`/api/admin/groups/${b.dataset.teachers}/teachers`),users=await api('/api/admin/users');openModal(t('teachers'),`<div class="table-wrap"><table><thead><tr><th>${t('name')}</th><th>${t('email')}</th><th>${t('permission')}</th><th>${t('actions')}</th></tr></thead><tbody>${assigned.map(x=>`<tr><td>${esc(x.name)}</td><td>${esc(x.email)}</td><td>${esc(x.permission)}</td><td><button class="danger-btn" data-remove-teacher="${x.teacher_id}" data-group="${b.dataset.teachers}">${t('remove')}</button></td></tr>`).join('')}</tbody></table></div>${teacherAssignForm(b.dataset.teachers,users,assigned)}`)}
 if(b.dataset.removeTeacher){await api(`/api/admin/groups/${b.dataset.group}/teachers/${b.dataset.removeTeacher}`,{method:'DELETE'});let trigger=document.querySelector(`[data-teachers="${b.dataset.group}"]`);if(trigger)trigger.click()}
 if(b.dataset.approveMember){await api(`/api/admin/groups/${b.dataset.group}/members/${b.dataset.approveMember}`,{method:'PATCH',body:{user_id:0,status:'active'}});$('#modal').close();toast(t('success'));go('groups')}
 }catch(x){error(x)}});
document.addEventListener('input',e=>{if(['statusFilter','taskFilter','workSearch'].includes(e.target.id)){let status=$('#statusFilter').value,task=$('#taskFilter').value,q=$('#workSearch').value.toLowerCase();let rows=state.work.filter(x=>(!status||x.status===status)&&(!task||String(x.task.id)===task)&&(!q||`${x.student.name} ${x.task.title} ${x.task.location}`.toLowerCase().includes(q)));$('#workTable').innerHTML=workTable(rows)}});
document.addEventListener('submit',async e=>{e.preventDefault();let f=e.target;try{
 if(f.id==='loginForm'){let d=dataObj(f),me=await api('/api/auth/login',{method:'POST',body:d});state.me=me;state.csrf=me.csrf_token;await showApp()}
 if(f.id==='registerForm'){let d=dataObj(f);if(!d.invite_code)delete d.invite_code;if(!d.group_name)delete d.group_name;await api('/api/auth/register',{method:'POST',body:d});toast(t('success'));$('[data-auth-tab=login]').click()}
 if(f.id==='groupForm'){let d=dataObj(f);d.is_active=!!f.elements.is_active.checked;await api('/api/groups',{method:'POST',body:d});$('#modal').close();toast(t('success'));go('groups')}
 if(f.id==='teacherAssignForm'){await api(`/api/admin/groups/${f.dataset.group}/teachers`,{method:'POST',body:{teacher_id:+f.elements.teacher_id.value,permission:f.elements.permission.value}});$('#modal').close();toast(t('success'));go('groups')}
 if(f.id==='taskForm'){let d=dataObj(f);d.capacity=d.capacity?+d.capacity:null;d.academic_group_id=d.academic_group_id?+d.academic_group_id:null;d.scheduled_at=d.scheduled_at?new Date(d.scheduled_at).toISOString():null;d.due_at=d.due_at?new Date(d.due_at).toISOString():null;d.is_open=!!f.elements.is_open.checked;let id=f.dataset.id;await api(id?`/api/tasks/${id}`:'/api/tasks',{method:id?'PUT':'POST',body:d});$('#modal').close();go('tasks')}
 if(f.id==='assignForm'){await api(`/api/tasks/${f.dataset.task}/assign`,{method:'POST',body:{student_id:+f.elements.student_id.value}});$('#modal').close();toast(t('success'));go('tasks')}
 }catch(x){let p=f.querySelector('.form-error');if(p)p.textContent=x.message;else error(x)}});
$('#logout').onclick=async()=>{try{await api('/api/auth/logout',{method:'POST'})}finally{state.csrf='';showAuth()}};
(async()=>{translate();try{let me=await api('/api/me');state.me=me;state.csrf=me.csrf_token;showApp()}catch{showAuth()}})();
